let R = parseInt(gets())
let V = 0

while(true){
  V = parseInt(gets())
  if(V > R){
    break
  }
}
 
let cont = 1
let it = R + 1;
while(true){
  cont++
  R += it
  it++
  if(R > V){
    console.log(cont)
    break;
  }
}
